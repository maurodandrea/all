{
  "devDependencies": {
    "@changesets/cli": "^2.29.0"
  },
  "name": "all",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "version": "changeset version && npm install",
    "tag": "changeset publish"
}
}
